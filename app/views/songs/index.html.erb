<% @songs.each do |song| %>
    <div>
      <div<%= link_to song.title, song_path(song) %></div>
      <div><%= song.release_year %></div>
      <div><%= song.artist_name %></div>
      <div><%= song.genre %></div>
    </div>
<% end %>